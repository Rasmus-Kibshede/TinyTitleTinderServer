

export interface AddressResponseDTO{
    city: string; 
    zipcode: string;
    address: string;
}

export interface AddressRequestDTO{
    city: string; 
    zipcode: string;
    address: string;
}